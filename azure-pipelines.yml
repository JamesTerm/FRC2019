# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master
- production

pool:
  name: 'Default'

steps:
- script: cd 
          gradlew generateVsCodeConfig  -Dorg.gradle.java.home="C:\Users\Public\wpilib\2020\jdk"
  displayName: 'Run a one-line script'